<%= form_for(@item) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_field :name, placeholder: "Create your new to-do item..." %>
  </div>
  <%= f.submit "Submit", class: "btn btn-primary" %>
<% end %>